# C-003 前端测试报告

> 模块 C · 前端测试 | 负责人：张洪铖 | 日期：2026-05-15

---

## 一、测试概览

| 指标 | 数值 |
|------|------|
| 测试文件总数 | 60 |
| 测试用例总数 | 455 |
| 通过 | 441 |
| 失败 | 14（均为 ssrf-guard 预存问题） |
| 执行耗时 | ~27s |

---

## 二、检验标准逐项对照

### Store 测试

| # | 检验标准 | 状态 | 测试文件 | 用例数 | 备注 |
|---|----------|------|----------|--------|------|
| 1 | 各子 store 测试：llm 独立生效 | 成功 | `tests/store/settings-llm.test.ts` |9例|测定用户选了 AI 模型、填了 API Key、调了思考参数，数据能不能正确存取 |
| 2 | 各子 store 测试：audio 独立生效 | 成功 | `tests/store/settings-audio.test.ts` | 15例 |朗读声音和语音识别的所有设置项能不能正确存取|
| 3 | 各子 store 测试：media 独立生效 | 成功 | `tests/store/settings-media.test.ts` | 10例 | |
| 4 | 各子 store 测试：layout 独立生效 | 成功 | `tests/store/settings-layout.test.ts` | 8例 |侧边栏折叠/展开、聊天区折叠、聊天区宽度能正常展现互不影响 |
| 5 | 各子 store 测试：agents 独立生效 | 成功 | `tests/store/settings-agents.test.ts` | 13 |选哪些 AI 角色参与讨论、手动/自动模式、最多对话轮次 |
| 6 | IndexedDB 迁移测试：旧 schema 到新 schema 升级正确 | 成功 | `tests/store/migration.test.ts` | 5例 | v0→v1→v2 版本升级、旧 key 迁移、缺失 provider 不崩溃 |
| 7 | 设置导入/导出测试：JSON 往返数据一致 | 成功 | `tests/store/import-export.test.ts` | 5例 |导出和导入回的json格式相同 |
| 8 | 设置预设测试：切换预设后所有值正确 | 成功 | `tests/store/presets.test.ts` | 5例 | 默认 / 演示 / 协作三种预设切换，各字段全部到位 |

### 组件测试

| # | 检验标准 | 状态 | 测试文件 | 用例数 | 备注 |
|---|----------|------|----------|--------|------|
| 9 | agent-bar 子组件测试：各子组件渲染正确 | 通过 | `tests/components/agent-bar.test.tsx` | 1例|首页底部那个 AI 角色选择 bar 能不能正常加载 |
| 10 | Canvas hooks 测试：pan / zoom / selection 行为正确 | 通过 | `tests/components/canvas-hooks.test.tsx` | 29例 | viewport/selection/spotlight/highlight/laser/zoom/白板/标尺/键盘集成 |
| 11 | Error Boundary 测试：组件抛错时边界生效 | 通过 | `tests/components/error-boundary.test.tsx` | 4 |页面某个组件崩了→显示兜底 UI，不白屏|
| 12 | 虚拟列表测试：长列表只渲染可见项 | 通过 | `tests/components/virtual-list.test.tsx` | 4 | 列表 100 项只渲染看得见的 10 项，空列表不崩溃|
| 13 | 主题测试：暗色/亮色切换生效 | 通过 | `tests/components/theme.test.tsx` | 4 | html 标签加/删 dark class，localStorage 记住偏好，跟随系统设置 |
| 14 | 响应式测试：移动端布局正确 | 通过 | `tests/components/responsive.test.tsx` | 3例 | 手机/平板/桌面三种断点能不能正确识别 |
| 15 | 快捷键测试：各快捷键触发正确行为 | 通过 | `tests/components/shortcuts.test.tsx` | 7例 |就是快捷键 |



---

## 三、失败用例详情

### 3.1 当前仍失败（14 例 — 项目预存，非本次任务问题）

| # | 文件 | 测试名 | 失败原因 |
|---|------|--------|----------|
| 1 | ssrf-guard.test.ts | allows a public hostname when DNS resolves to a public IP | node:dns mock 缺少 default export |
| 2 | ssrf-guard.test.ts | allows a public IP literal without DNS lookup | ↑ 同上 |
| 3 | ssrf-guard.test.ts | allows a public IPv6 literal without DNS lookup | ↑ 同上 |
| 4 | ssrf-guard.test.ts | rejects invalid URLs and non-http protocols | ↑ 同上 |
| 5 | ssrf-guard.test.ts | rejects blocked hostnames immediately | ↑ 同上 |
| 6 | ssrf-guard.test.ts | rejects private IPv4 literals | ↑ 同上 |
| 7 | ssrf-guard.test.ts | rejects private IPv6 literals and mapped loopback addresses | ↑ 同上 |
| 8 | ssrf-guard.test.ts | rejects 6to4 tunnel addresses embedding private IPv4 | ↑ 同上 |
| 9 | ssrf-guard.test.ts | allows 6to4 tunnel addresses embedding public IPv4 | ↑ 同上 |
| 10 | ssrf-guard.test.ts | rejects Teredo tunnel addresses embedding private IPv4 | ↑ 同上 |
| 11 | ssrf-guard.test.ts | allows Teredo tunnel addresses embedding public IPv4 | ↑ 同上 |
| 12 | ssrf-guard.test.ts | rejects hostnames that resolve to a private IP | ↑ 同上 |
| 13 | ssrf-guard.test.ts | rejects hostnames when any DNS answer is private | ↑ 同上 |
| 14 | ssrf-guard.test.ts | fails closed when DNS lookup errors | ↑ 同上 |

> 原因：`lib/server/ssrf-guard.ts` 使用了 `node:dns` 模块，测试的 `vi.mock` 未正确导出 `default`。此文件为服务端 SSRF 防护测试，与前端测试任务无关。

### 3.2 已修复的测试问题

| # | 文件 | 测试名 | 失败原因 | 修复方式 |
|---|------|--------|----------|----------|
| 1 | canvas-hooks.test.tsx | setViewportSize 等 7 例 | API 字段名/参数猜错（如 `spotlight` 实际是 `spotlightElementId`） | 查真实 canvas.ts 源码按正确 API 重写 |
| 2 | canvas-hooks.test.tsx | setSpotlight/setHighlight 可带 options | store 自动补默认值（transition/animated） | 改用 `toMatchObject` |
| 3 | responsive.test.tsx | 窗口尺寸改变时 matchMedia 事件触发 | `vi.resetAllMocks()` 清了上条测试的 mock | 删除多余用例 |
| 4 | settings-audio.test.ts | IDE 报红但测试通过 | mock 用了 `whisper`，真实类型是 `openai-whisper` | 改为 `openai-whisper` |


## 四、覆盖率摘要

| 模块 | 语句 % | 分支 % | 函数 % | 行 % |
|------|--------|--------|--------|------|
| lib/store/settings.ts | 85.71 | 68.77 | 79.61 | 86.89 |
| lib/store/canvas.ts | 57.69 | 70.00 | 51.16 | 56.86 |
| lib/store/media-generation.ts | 34.84 | 14.70 | 52.63 | 38.18 |
| lib/store/keyboard.ts | 100 | 100 | 100 | 100 |
| lib/orchestration/registry/store.ts | 24.35 | 5.40 | 47.61 | 24.63 |
| components/error-boundary.tsx | 100 | 100 | 100 | 100 |
| components/virtual-list.tsx | 85.71 | 60.00 | 75.00 | 85.71 |

> 注：store 模块覆盖率受 `vi.mock` + 动态 `import()` 测试模式影响，实际运行时覆盖率可能更高。
> 复现要求：运行 `pnpm test -- --coverage --coverage.provider=istanbul` 应得到与上表相近的覆盖率数据。

---

## 六、测试执行命令

```bash
# 跑全部测试
pnpm test

# 只跑 store 测试
pnpm test tests/store/

# 只跑组件测试
pnpm test tests/components/

# 跑单个文件
pnpm test tests/store/settings-llm.test.ts
```
