# B-005 幻灯片讲解页 UI — 开发规划

> 任务编号: B-005 | 负责人: 队员2 | 状态: ✅ 已完成

## 需求

独立页面：左侧操作区 + 右侧幻灯片展示 + 虚拟形象播放器，串联 B-001/B-002/B-003 全部功能。

## 架构

```
app/sky/slides/page.tsx
  ├── 顶部导航栏（三栏布局）
  │     ├── 左：S logo + 页面标题
  │     ├── 中：搜索输入框 + 难度切换 + 生成按钮
  │     └── 右：状态提示
  ├── 左侧面板（AvatarSelector + ThumbnailList）
  └── 右侧主区域
        ├── SlideViewer（复用 BaseElement 组件）
        ├── AvatarPlayer（右下角玻璃叠加层）
        └── 底部操作栏（翻页 + 重新生成 + 导出 PPTX）
```

## 涉及文件

| 文件 | 操作 |
|------|------|
| `components/sky/slides/slide-viewer.tsx` | 重写 |
| `components/sky/slides/thumbnail-list.tsx` | 新建 |
| `components/sky/slides/avatar-selector.tsx` | 新建 |
| `components/sky/slides/avatar-player.tsx` | 重写 |
| `app/sky/slides/page.tsx` | 重写 |

## 检验标准

- [x] 左侧：主题输入框 + 难度选择 + 生成按钮
- [x] 左侧下方：幻灯片缩略图列表（可点击切换）
- [x] 右侧：幻灯片展示区（复用 BaseElement）
- [x] 右下角：虚拟形象头像 + 讲解文字 + 播放控制
- [x] 底部操作栏：导出 PPTX、重新生成、翻页
- [x] 滚动条隐藏
- [x] 键盘翻页（← →）
- [x] 响应式适配
