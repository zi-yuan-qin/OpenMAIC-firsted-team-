# Module C · 天空课堂变更清单

> 负责人：队员3 | 日期：2026-05-18 | 基于 `task/zhimo-design.md`

## 变更总览

| 类型 | 数量 |
|------|------|
| 新建文件 | 13 |
| 重写文件 | 6 |
| 修改文件 | 2 |

---

## C-001 错题本

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `lib/mistakes/db.ts` | IndexedDB 封装（数据库 `sky-classroom`，objectStore `mistakes`），提供 `getDB` / `addMistake` / `getAllMistakes` / `updateMistake` / `deleteMistake` / `clearAll` |
| 新建 | `lib/mistakes/mistake-tracker.ts` | 错题追踪器：`trackMistake`（写入 DB）、`getMistakes`（4 种筛选）、`markReviewed`、`deleteMistake`、`getMistakeStats`（统计）、`exportMistakes`（导出） |
| 新建 | `lib/mistakes/cause-analyzer.ts` | 规则引擎错因诊断：`analyzeMistakeCause`（7 条优先级规则，6 种错因类型）、`extractNumbers`（数字提取辅助函数） |
| 修改 | `lib/mistakes/index.ts` | 新增 value exports（db / mistake-tracker / cause-analyzer / knowledge-graph） |

## C-002 知识图谱

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `lib/mistakes/knowledge-graph.ts` | `buildKnowledgeGraph`（节点+边+薄弱点）、`recommendLearningPath`（从最薄弱点出发的 DFS+BFS 路径）、`getMasterySummary`（整体掌握度统计） |

## C-003 AI 助手

| 操作 | 文件 | 说明 |
|------|------|------|
| 重写 | `components/sky/assistant/assistant-fab.tsx` | 悬浮球入口：接入 store 的 `isOpen`/`toggleOpen`，右下角固定定位，未展开时 56x56 圆形按钮，展开后渲染面板+配置 |
| 重写 | `components/sky/assistant/assistant-panel.tsx` | 多轮对话面板：消息列表+输入框+mock LLM 响应（3 类关键词匹配，8 种预设回复），消息 >20 条自动调用 `compressConversation` 压缩 |
| 重写 | `components/sky/assistant/assistant-config.tsx` | 配置面板：学科（5 项）/ 风格（3 项）/ 难度（3 项）选择器，天空蓝选中态高亮 |
| 新建 | `lib/orchestration/registry/templates/solvers/universal.ts` | 通用解题助手模板 |
| 新建 | `lib/orchestration/registry/templates/solvers/science.ts` | 理科专精助手模板 |
| 新建 | `lib/orchestration/registry/templates/solvers/humanities.ts` | 文科综合助手模板 |

## C-004 学习数据页 UI

| 操作 | 文件 | 说明 |
|------|------|------|
| 重写 | `components/sky/learning/mistake-book.tsx` | 错题本列表：筛选栏（错因/复习状态/知识点搜索）+ 卡片列表（题目截断、答案对比、错因彩色标签、操作按钮）+ 空状态 |
| 重写 | `components/sky/learning/knowledge-graph-view.tsx` | 知识图谱可视化：纯 Canvas 力导向图（100 次迭代，排斥力+弹力+向心力），节点大小=掌握度，颜色=红黄绿，薄弱点红色虚线，点击 tooltip，High-DPI 支持 |
| 重写 | `components/sky/learning/learning-stats.tsx` | 学习统计面板：3 个统计卡片（总错题/已复习/薄弱点）+ 错因分布条形图（纯 CSS）+ 空状态 |

## C-005 模块 C 测试

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `tests/mistakes/mistake-tracker.test.ts` | 33 用例：IndexedDB CRUD（7）、trackMistake（2）、getMistakes 筛选（7）、markReviewed（5）、getMistakeStats（5）、exportMistakes（5）、集成场景（2） |
| 新建 | `tests/mistakes/cause-analyzer.test.ts` | 18 用例：extractNumbers（6）、analyzeMistakeCause 全覆盖 6 种错因（12） |
| 新建 | `tests/mistakes/knowledge-graph.test.ts` | 16 用例：buildKnowledgeGraph（8）、getMasterySummary（3）、recommendLearningPath（4）+ 边界（空数组、mastery 保底） |
| 新建 | `tests/sky/assistant.test.tsx` | 8 用例：Fab 开关、Panel 欢迎消息/快捷提示/输入框、Config 学科/风格/难度按钮 |
| 新建 | `tests/sky/mistake-book.test.tsx` | 4 用例：空状态、有数据渲染、错因筛选按钮、复习状态切换 |
| 新建 | `tests/sky/learning-stats.test.tsx` | 3 用例：空状态、统计卡片、错因分布条形图 |
| 新建 | `tests/sky/dashboard.test.tsx` | 5 用例：StatCards 空/有数据、QuickActions 4 个入口、ActivityTimeline 空/有数据 |

## C-006 首页仪表盘

| 操作 | 文件 | 说明 |
|------|------|------|
| 重写 | `app/sky/page.tsx` | 首页仪表盘：4 时段问候语 + StatCards + QuickActions + ActivityTimeline |
| 重写 | `components/sky/dashboard/stat-cards.tsx` | 3 个导出组件：StatCards（今日搜题/待复习/掌握度）、QuickActions（4 个 Next.js Link 快捷入口）、ActivityTimeline（最近 5 条，相对时间） |

## Store 激活

| 操作 | 文件 | 说明 |
|------|------|------|
| 重写 | `lib/store/use-sky-classroom-store.ts` | 4 个 slice（Solve / Slides / Mistakes / Assistant）的 14 个 action 全部从 `log.debug` 改为真实 Zustand `set()` 操作。保持接口签名和 persist 配置不变 |

---

## 测试结果

| 指标 | 数值 |
|------|------|
| 新增测试文件 | 7 |
| 新增测试用例 | 87 |
| 新增用例通过率 | 100%（87/87） |
| 全量测试 | 109 文件，1305 用例，1209 通过 |
| 构建状态 | 模块 C 代码零 TypeScript 错误 |

> 全量测试中 96 个失败用例全部为上游 P6-001 集成测试的预存问题，与模块 C 无关。
